interface:
  display_name: "Lumina Stream"
  short_description: "Repo-specific patterns and workflows for lumina-stream"
  default_prompt: "Use the lumina-stream repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true